<Page 
    loaded="onLoaded" 
    navigatingTo="onNavigatingTo" 
    class="page" actionBarHidden="true" backgroundSpanUnderStatusBar="false" xmlns="http://www.nativescript.org/tns.xsd">

    <FlexboxLayout class="page">
        <StackLayout class="form">
            <Image class="logo" src="res://logo" />
            <Label class="header" text="KIKAN" />
            <Label class="sub-header" text="Kirim ikan" />

            <GridLayout rows="auto, auto, auto">
                <StackLayout row="0" class="input-field">
                    <TextField class="input" text="{{ email }}" hint="Email" keyboardType="email" autocorrect="false" autocapitalizationType="none" returnKeyType="next" />
                    <Label class="hr-light" />
                </StackLayout>

                <StackLayout row="1" class="input-field">
                    <TextField id="password" class="input" text="{{ password }}" hint="Password" secure="true" />
                    <Label class="hr-light" />
                </StackLayout>
            </GridLayout>

            <Button text="Log In" class="btn btn-primary m-t-20" tap="login" />
            <Label text="Forgot your password? Reset Now Here" class="login-label" tap="forgotpassword" />
            <Label text="Sign up for New Customer" class="login-label m-t-10" tap="signup" />
        </StackLayout>
    </FlexboxLayout>
</Page>